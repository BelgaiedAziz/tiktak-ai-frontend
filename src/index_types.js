// Central exports for types
// Réexporte tous les types définis
export {};

// Import types JSDoc since TypeScript is not used
// Lors de l'utilisation:
// import { TemplatesHookResult } from './types'
// const { templates, loading, error } = useTemplates();
